name: KeepAlive

on:
  schedule:
    - cron: "*/6 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl -sSf "$HEALTHCHECK_URL" > /dev/null
        env:
          HEALTHCHECK_URL: ${{ secrets.HEALTHCHECK_URL }}
